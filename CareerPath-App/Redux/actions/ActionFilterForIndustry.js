const RadioSortBy = text => {
  return {
    type: 'RadioSortBy',
    resLabel: text,
  };
};

const RadioIndustry = text => {
  return {
    type: 'RadioIndustry',
    resLabel: text,
  };
};

const PriceIndustry = text => {
  return {
    type: 'PriceIndustry',
    resLabel: text,
  };
};
